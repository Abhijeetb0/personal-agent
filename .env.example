# Personal Agent — multi-user, 100% free stack.
# Secrets chat me mat bhejo — seedha .env / Render / Vercel dashboard me dalo.

# --- Supabase (Project Settings → Data API) ---
SUPABASE_URL=https://xyzcompany.supabase.co
SUPABASE_PUBLISHABLE_KEY=eyJ...publishable...
SUPABASE_SECRET_KEY=eyJ...secret... (sirf agent backend me, web me MAT dalo)

# --- AI (dono free; Groq primary, Gemini fallback) ---
GROQ_API_KEY=gsk_...
# GROQ_MODELS priority order me (comma-separated). 429 aate hi agle model pe turant failover.
# Purana GROQ_MODEL (single) bhi chalega. Khali chhoda to built-in default list chalegi.
GROQ_MODELS=groq/compound-mini,openai/gpt-oss-120b,openai/gpt-oss-20b,qwen/qwen3.6-27b
# Live list check: curl https://api.groq.com/openai/v1/models -H "Authorization: Bearer $GROQ_API_KEY"
# (Groq purane models retire karta rehta hai — 404 aaye to list se sahi ID uthao)
GEMINI_API_KEY=AIza...
# AI_ORDER brain ka provider order hai (dono tools samet chalte hain). Jiski key nahi, wo auto-skip.
AI_ORDER=groq,gemini

# --- Agent (Render) ---
# WAKE_SECRET: /wake pinger (UptimeRobot) ka password — bina iske /wake 503 dega.
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Render env me dalo + UptimeRobot URL me ?key= lagao — git me MAT dalo.
WAKE_SECRET=lamba_random_hex_yahan
# AGENT_ENC_KEY: WaSession.auth_blob encryption (bina iske plaintext save hoga).
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Render/Railway env me dalo — DB me MAT dalo, git me MAT dalo.
AGENT_ENC_KEY=64_hex_chars_yahan
# OWNER_NUMBER sirf local dev fallback hai; production me har user dashboard se set karta hai
OWNER_NUMBER=91XXXXXXXXXX
PORT=3001
# Local dev me bina login test karna ho to: ALLOW_NO_AUTH=1 (production me MAT lagana)

# --- Web (Vercel) ---
AGENT_BASE_URL=https://tumhara-agent.onrender.com
NEXT_PUBLIC_SUPABASE_URL=https://xyzcompany.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=eyJ...publishable...
